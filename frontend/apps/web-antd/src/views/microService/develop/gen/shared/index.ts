/**
 * 代码生成模块 - 共享配置
 * 从全局共享配置导出，并补充模块特有配置
 */

// 从全局共享配置导出通用配置
export {
  booleanDict as createBooleanDict,
  createTimeColumn,
  datetimeValueBuilder,
  hiddenIdColumn,
  resetFormValues,
  searchConditionDict,
} from '#/plugin/fast-crud/shared';
